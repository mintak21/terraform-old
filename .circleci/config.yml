version: 2.1

orbs:
  terraform: ovotech/terraform@1.6.6

workflows:
  version: 2
  ci:
    jobs:
      - format

jobs:
  format:
    executor: terraform/terraform-0_12
    parameters:
      path: files
    steps:
      - run:
          name: tf files format check
          command: fmt-check
